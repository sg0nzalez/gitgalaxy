spec:
  inputs:
    target:
      default: '.'
      description: "The directory to scan."
---
gitgalaxy_scan:
  image: python:3.10-slim
  script:
    - pip install gitgalaxy
    - galaxyscope $[[ inputs.target ]]
